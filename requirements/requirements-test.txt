# requirements-test.txt — Dependencias para la suite de tests
#
# Instalación:
#   pip install -r requirements/requirements-test.txt
#
# Para ejecutar la suite completa:
#   pytest tests/ -v
#
# Para ejecutar solo los tests de Playwright (UI):
#   pytest tests/test_dashboard.py -v
#
# Para omitir los tests de Playwright (solo pytest/backend):
#   pytest tests/ -v --ignore=tests/test_dashboard.py

# ── Framework de tests ────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0          # Cobertura de código

# ── Tests de UI con Playwright ────────────────────────────────────────────────
# Requiere además: playwright install chromium
pytest-playwright>=0.5.0,<1.0.0

# ── Mocking ───────────────────────────────────────────────────────────────────
# unittest.mock viene incluido en Python >= 3.3, no hace falta instalar nada extra

# ── Análisis estadístico para tests de drift ─────────────────────────────────
scipy>=1.12.0,<2.0.0

# ── Dependencias compartidas con el proyecto (ya instaladas) ──────────────────
# Las siguientes se asumen instaladas; no se repiten para evitar conflictos:
#   scikit-learn, pandas, numpy, joblib (requirements-training.txt)
#   fastapi, httpx, pydantic (requirements.txt) — httpx es necesario para TestClient
httpx>=0.26.0,<0.28.0             # Cliente HTTP para FastAPI TestClient
pytest-subtests>=0.13.0           # Soporte para subtests en pytest
anyio[trio]>=4.0.0,<5.0.0        # Backend async requerido por starlette TestClient
